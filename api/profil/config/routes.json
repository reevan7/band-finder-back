{
  "routes": [
    {
      "method": "GET",
      "path": "/profils",
      "handler": "Profil.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/profils/count",
      "handler": "Profil.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/profils/:_id",
      "handler": "Profil.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/profils",
      "handler": "Profil.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/profils/:_id",
      "handler": "Profil.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/profils/:_id",
      "handler": "Profil.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}